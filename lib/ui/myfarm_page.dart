import 'package:flutter/material.dart';

class MyFarmPage extends StatelessWidget {
  const MyFarmPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MyFarm Page'),
      ),
    );
  }
}
